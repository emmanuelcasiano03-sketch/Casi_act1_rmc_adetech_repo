# Your Repository Name
Description of your project

## 🚀 Activity 2: Simple Node.js Server

This is a simple Node.js HTTP server that serves an HTML page.

### How to run:
1. Make sure Node.js is installed
2. Run: `node server.js`
3. Visit: `http://127.0.0.1:3000`

### Features:
- Built with native Node.js `http` module
- Serves a styled HTML response
- Console logging for server status

Created by Emmanuel Casiano

## 🚀 Activity 2: Simple Node.js Server

This is a simple Node.js HTTP server that serves an HTML page.

### How to run:
1. Make sure Node.js is installed
2. Run: `node server.js`
3. Visit: `http://127.0.0.1:3000`

### Features:
- Built with native Node.js `http` module
- Serves a styled HTML response
- Console logging for server status

Created by Emmanuel Casiano
